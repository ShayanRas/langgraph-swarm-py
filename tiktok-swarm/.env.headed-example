# Example configuration for running headed browsers in Docker
# Copy this to .env if you want to use non-headless browsers

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# MS Token Encryption
MS_TOKEN_ENCRYPTION_KEY=your_generated_encryption_key_here

# TikTok Anti-Bot Detection Settings - Headed Mode
TIKTOK_STEALTH_LEVEL=aggressive  # Aggressive stealth for best anti-detection
TIKTOK_HEADLESS=false  # Non-headless for better stealth (requires Xvfb)
TIKTOK_BROWSER=chromium  # Chromium works best with Xvfb
TIKTOK_RANDOM_BROWSER=false  # Stick to chromium for headed mode
TIKTOK_MAX_SESSIONS_PER_USER=2
TIKTOK_SESSION_TIMEOUT=300

# Note: When TIKTOK_HEADLESS=false in Docker:
# - The container will automatically start Xvfb virtual display
# - This uses more resources (~400MB per session)
# - Browser windows run invisibly on the virtual display
# - Better bot detection evasion but slower